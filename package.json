{
  "name": "typo3-agentation",
  "version": "1.4.0",
  "description": "Vite build for the TYPO3 agentation extension: bundles agentation + React into one self-contained toolbar script.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build --mode production"
  },
  "dependencies": {
    "agentation": "^3.1.2",
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "engines": {
    "node": ">=22.12"
  }
}
