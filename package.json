{
  "name": "typo3-agentation",
  "version": "0.1.0",
  "description": "Vite build for the TYPO3 agentation extension — bundles agentation + React into a self-contained, framework-agnostic toolbar.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build --mode production"
  },
  "dependencies": {
    "agentation": "^3.0.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
