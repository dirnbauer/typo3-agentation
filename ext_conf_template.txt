# cat=basic/enable; type=string; label=Agentation API key (stores annotations in the Agentation cloud; leave empty to sync through a local agentation-mcp server). Stays on the server: the backend proxy and the generated MCP configuration use it, the browser never sees it.
apiKey =

# cat=basic/enable; type=string; label=Project identifier (optional; sent with webhook submissions so the receiver can tell installations apart)
workspaceId =

# cat=basic/enable; type=string; label=Sync endpoint (leave empty for auto: cloud when an API key is set, else http://localhost:4747 via the backend proxy)
syncEndpoint =

# cat=basic/enable; type=boolean; label=Enable in frontend (per backend user via User Settings and the Admin Panel section)
frontendEnabled = 1

# cat=basic/enable; type=boolean; label=Enable in backend (per backend user via User Settings)
backendEnabled = 1

# cat=basic/enable; type=options[Development,Development and Testing,All contexts]; label=Application context gate
contextGate = Development

# cat=basic/enable; type=boolean; label=Default opt-in for users who never saved their Agentation user settings
defaultOptIn = 0

# cat=advanced/toolbar; type=options[bottom-right,bottom-left,top-right,top-left]; label=Toolbar position
toolbarPosition = bottom-right

# cat=advanced/toolbar; type=string; label=Webhook URL (optional; annotations are POSTed here on submit)
webhookUrl =

# cat=advanced/toolbar; type=string; label=Additional toolbar options as a JSON object (merged into the toolbar props)
additionalOptions =
