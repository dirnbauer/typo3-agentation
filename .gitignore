/node_modules/
/vendor/
/var/
/.phpunit.cache/
/.phpstan.cache/
/composer.lock
/package-lock.json
.DS_Store
.idea/

# Resources/Public/Vite/ is intentionally committed so composer/VCS
# installations get the built assets without a node toolchain.
# Regenerate locally with `npm install && npm run build` before pushing.
