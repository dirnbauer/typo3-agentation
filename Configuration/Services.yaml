services:
  _defaults:
    autowire: true
    autoconfigure: true
    public: false

  Webconsulting\Agentation\:
    resource: '../Classes/*'
    exclude: '../Classes/Enum/*'

  # Public services are declared with #[Autoconfigure(public: true)] on the
  # classes themselves (backend controllers, Admin Panel module).
